import Filters from './Filters.jsx';

export default function Header() {
    return(
        <header>
            <h1>
                React shop
            </h1>
            <Filters/>
        </header>
    );
}